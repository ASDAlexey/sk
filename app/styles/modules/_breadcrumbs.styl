.wrapper-breadcrumbs{
  padding-top:23px;
  &.color{
    background-color:#d8e0d5;
  }
}
.breadcrumbs{
  width:978px;
  padding:0 11px;
  margin:0 auto;
  > li{
    margin-left:30px;
    &:first-child{
      margin-left:0;
    }
    dibt();
    a, span{
      display:block;
      lb(14px);
    }
    a{
      color:#f5f5f5;
      position:relative;
      z-index:0;
      &:hover{
        text-decoration:none;
      }
      &:after{
        top:50%;
        tt(0, -50%)
        right:-22px;
        content:'';
        position:absolute;
        z-index:1;
        sprite($ba);
      }
    }
    span{
      lm();
      color:#f5f5f5;
    }
  }
}
.breadcrumbs-block
  position:relative
  z-index:0
  .wrapper-breadcrumbs
    position:absolute
    z-index:1
    left:0
    top:0
